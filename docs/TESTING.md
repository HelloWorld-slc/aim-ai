# 测试记录

## 0.3.0 · 流式回答与差异展示

2026-09-20，同一 Windows 11 测试环境：

- 39 项核心测试：新增多字节/分段 SSE、提前到达的思考与回答、分段工具参数、Claude 思考签名及加密块透传、断流/取消、代码块检测、行数差异、流式历史检查点。
- 22 项实际 VS Code 宿主检查：新增自动差异页、只读快照、实际模型名、流式思考、完整代码块识别和中断保留。第二次启动恢复 34 条记录，包括模型和思考文本。
- 独立 Edge 界面测试：Markdown 标题/表格/高亮、复制、链接、注入拦截、思考展开、流式更新无重复消息、模型署名、增删统计、240 px 布局。已检查界面截图。
- 所有模型响应来自本机模拟服务；没有使用用户 API Key，没有进行机器人操作。真实服务的流式兼容性仍需用户使用自己的账户验证。


## 0.2.0 · 模型服务、思考强度与历史记录

2026-09-20，在同一 Windows 11 测试机验证：

- TypeScript 类型检查与构建通过；31 项核心测试通过，覆盖原子历史存储、并发追加、重命名/删除、损坏文件保留、上下文长度限制、七家预设、协议映射、思考档位与 Claude/兼容接口工具续接签名。
- 18 项实际 VS Code 宿主检查通过；思考设置传入本地模拟 API，无效档位拒绝；新对话保留旧记录，打开旧记录能继续提问。
- 关闭首个 VS Code 测试进程后再启动第二个，恢复原对话的 20 条记录及所选思考强度；不恢复旧代码建议的可应用状态。
- 独立无界面 Edge 检查七个图标加载、预设入口、思考菜单、历史搜索/切换/重命名/删除入口、独立草稿、导出后打开开关、31 篇资料及五个页签 240 px 布局。界面截图已检查。
- 使用实际启动函数把无运动测试项目交给已安装的 VEXcode AIM 4.67.0；原应用窗口显示了新项目标题及对应源码，确认自动打开成功。没有点击下载或运行。
- 未使用真实 API 密钥，未访问远程模型服务，未连接或移动机器人；每家服务的真实账户与工具调用仍需用户参与联调。


## 0.1.1 · Python 资料扩充

2026-09-20，在同一 Windows 11 测试机验证：

- TypeScript 类型检查、构建和 Webview JavaScript 语法检查通过。
- 21 项核心测试通过。新增覆盖 15 个 Python 分类、12 个 Logic 子页、31 篇文档完整性及内部链接、14 个 MicroPython 模块入口、中文与完整方法名检索、上下文长度上限。
- 16 项实际 VS Code 扩展宿主测试通过。新增验证完整目录可加载，以及本地模拟模型通过 `read_reference` 读取 Timer 子页并获得正文及来源，源码保持不变。
- 新建无界面 Edge 测试会话，使用模拟 VS Code 消息桥检查资料页面：默认分组、展开全部 31 项、中英文与方法名搜索、点击条目、状态更新保留搜索、无匹配提示、恢复展开状态、240 px 窄栏无横向溢出、无脚本错误。已检查生成的页面截图。
- 本次未访问真实模型 API、未连接或操作机器人。界面测试使用独立浏览器环境，不接入用户正在使用的 Edge 会话。

核心检查：`npm run typecheck`、`npm test`。扩展宿主：`node tests/run-integration.cjs`。可选界面测试：安装 Playwright 后执行 `node tests/webview.cjs`（或将 `AIM_AI_PLAYWRIGHT` 指向已有的 Playwright 模块），需本机 Edge。

测试结果文件位于 `test-results`，不会打包进入插件。资料为 API 索引与中文速查；静态检查不等于所有接口已做实机验证。

## 0.1.0 原始验收记录

日期：2026-09-20。测试机：Windows 11 家庭版 64 位（10.0.26200）、VS Code 1.137.0、Node.js 24.19。目标包括 Windows 10，但尚未在独立 Windows 10 机器验收。

## 自动化

- TypeScript 类型检查与扩展构建通过。
- 18 项核心测试通过：格式与元数据往返、槽位转换、官方生成区注释兼容、路径边界、平台混用、接口地址与凭据、请求取消、响应大小限制、Python语法检查等。
- 14 项实际 VS Code 扩展宿主测试通过：激活、命令、语法检查、本地模拟 API、建议不自动改代码、原生差异编辑器、未保存缓冲区修改、恢复、并发编辑保护、导出源码一致性、槽位转换、连续保存自动导出、侧栏创建、资料检索。
- 自动化测试没有访问真实模型服务，不消耗用户 API 额度，没有连接机器人。

## 界面检查

在独立的 VS Code 开发宿主窗口检查了“编程”“项目与设备”“AIM资料”三个页签：侧栏正常显示，窄栏按钮可换行，下载/运行在默认设置下不可用。点击“足球视觉与 AprilTag”可打开对应本地资料。未操作用户已有 VS Code 窗口。

## 官方应用互通

VEXcode AIM 桌面版 4.67.0 成功打开 `AIM_Square.aimpython`：100 mm、20% 速度、4 次前进/90度转向和最后停止全部显示完整。导出槽位 1 修正后在官方应用显示槽位 1。

官方应用保存后的项目重新通过插件解析和导出，机器人初始化语句只有一次。已兼容官方应用重复添加相邻 `#region` 起始注释的行为。官方应用会重新生成配置区，因此不要求整个源码字符串与导出前一致；用户动作区应保持一致。

## 尚待验证

- 真实服务商模型的函数工具调用、限流与计费信息。
- 官方 VEX 扩展 0.8.2026020401 与 AIM 固件的设备连接/下载/停止/运行。现有桥接仅核对了本地扩展命令，未做实机验证。
- 正方形实际轨迹、足球视觉数据、双机消息和比赛流程。
- Edge 网页版真实导入（当前完成的是桌面版验证）。
- 蓝牙直连下载和官方应用实时双向同步未实现。

静态语法通过、模拟接口通过、官方应用能打开项目，都不等于机器人已执行成功。
